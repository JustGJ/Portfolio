import React from 'react';

interface IProps {
    type: string;
    icon: string;
    text: string;
}

const Technos = ({ type, icon, text }: IProps) => {
    return <div></div>;
};

export default Technos;
